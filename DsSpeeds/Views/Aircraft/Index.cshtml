@model IEnumerable<Read.Models.AircraftReadModel>

@{
    ViewBag.Title = "All Countries";
}

<h2>All Aircraft</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AircraftName)
        </th>
        <th>
            Best Speed
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AircraftName)
        </td>
        <td>
            @{
                if (item.AllVerifiedSpeeds == null || item.AllVerifiedSpeeds.Count < 1)
                {
                    @("[No speeds]")
                }
                else
                {
                    @(item.AllVerifiedSpeeds.Max(s => s.SpeedInMilesPerHour))
                }
            }
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.Id }) 
        </td>
    </tr>
}

</table>
